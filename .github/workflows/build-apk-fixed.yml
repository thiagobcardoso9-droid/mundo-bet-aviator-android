name: Gerar APK FINAL COM LOGO

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use o workflow Gerar APK MUNDO BET AVIATOR para a compilação automática oficial."
